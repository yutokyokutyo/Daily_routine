require 'spec_helper'

describe GemSamplePruby do
  it 'has a version number' do
    expect(GemSamplePruby::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
